version: "2"
services:
    server:
        build: .
        external_links:
            - ":"
        ports:
            - "80:80"
            - "443:443"
        volumes:
            - "./conf:/etc/nginx/conf.d/"
            - "./ssl:/etc/nginx/ssl/"
networks:
    default:
        external:
            name: main