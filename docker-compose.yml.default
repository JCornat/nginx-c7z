version: '3'
services:
    server:
        restart:
        build: .
        external_links:
            - ':'
        ports:
            - '80:80'
            - '443:443'
        volumes:
            - './conf:/etc/nginx/conf.d/'
            - './nginx.conf:/etc/nginx/nginx.conf'
            - './ssl:/etc/nginx/ssl/'
networks:
    default:
        external:
            name: main
